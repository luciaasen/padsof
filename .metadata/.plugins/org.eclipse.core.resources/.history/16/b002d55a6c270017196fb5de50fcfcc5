/**
 * 
 */
package icfs.login;

import java.awt.*;
import java.awt.event.*;

import javax.swing.*;

/**
 * @author lusilu
 *
 */
public class LuciasTrial {
	public static void main(String []args){
		JFrame loginWindow = new JFrame("moon");
		Container cont = loginWindow.getContentPane();
		cont.setLayout(new SpringLayout());
		
		JPanel mainBox = new JPanel();
		JLabel text = new JLabel("Username");
		JL
		text.setVisible(true);
		cont.add(text, BorderLayout.CENTER);
		
		cont.setBackground(Color.getHSBColor(60,50, 60));
		cont.setSize(300, 300);
		cont.setVisible(true);
		
		loginWindow.setVisible(true);
		loginWindow.setSize(500, 500);
	}
	private JPanel login(){
		JPanel login = new JPanel();
		
		login.setLayout(new SpringLayout());
	}
}
